{% extends 'base.html.twig' %}
{% block content1 %}
  <div class="section section-breadcrumbs">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <h1>Tag {{attribute(tag, 'nom' ~ app.request.locale)}} </h1>
        </div>
      </div>
    </div>
  </div>

  {% set works = tag.works %}
  <div class="section">
    <div class="container">
      <div class="row" id="more-ajax">
        {% include 'works/_liste.html.twig' %}
      </div>
    </div>
  </div>
{% endblock %}
